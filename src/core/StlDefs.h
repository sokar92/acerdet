#ifndef _AcerDet_Core_STL_Defs_
#define _AcerDet_Core_STL_Defs_

#pragma once

#include <vector>
#include <queue>
#include <stack>
#include <algorithm>
using namespace std;

//#define _for (n,i) for(int i=0;i<1;i++)
//#define _for (a,b,i) for(int i=a;i<=b;i++)

//#define _foreach (vec,it) for(vec::const_iterator it = vec.begin(); it != vec.end; it++)

#endif
