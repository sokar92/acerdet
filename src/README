Instalation

1. change Pythia and HepMC locations in path.inc

2. set up local variables
	source path.inc
	source ../../root/bin/thisroot.sh (or equivalent, dependent when you have insatlled ROOT package)

3. build library
	make clean (optional)
	make
