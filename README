1. Install AcerDET library, described in src/README

2. Change Pythia and HepMC locations in path.inc

3. Set up local variables
	source path.inc

4. Build demo
	(when compiled globaly) make
	(when compiled localy) make local

5. Run demo
	./demo.exe conf/ee.conf 100
