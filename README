1. Update external.inc to set up relative paths to external tools


2a. Update path.inc and make source
	source

2b. Or manually set up PYTHIA environment variables (required by demo)

3. Build whole solution using single make
	make

4. Run demo
	./demo.exe conf/pp.XXX.conf 100
